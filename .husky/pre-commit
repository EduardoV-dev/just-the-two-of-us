#!/usr/bin/env sh

# Run lint-staged (ESLint + Prettier on staged files)
pnpm lint-staged

# TypeScript type checking
pnpm -r typecheck

# Full production build — commit blocked if build fails
pnpm -r build
